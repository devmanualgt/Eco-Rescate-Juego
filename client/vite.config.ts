import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    port: 5000, // Cambia este valor al puerto que prefieras (ej: 5173, 8080, etc.)
    open: true, // Opcional: Abre el navegador automáticamente
  },
});
